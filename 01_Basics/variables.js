const empId="123";            //double quote optional 
let empSalary;         //double quote optional + +  initialosed(optionl)

var empAddress="Pune";        //double quote manadatory + NOt recommended to use due to scope issue + must be initialosed(else give error)
empDesignation="Manager";     //double quote manadatory + must be initialosed(else give error)

console.table([empId, empSalary, empAddress,empDesignation ]);